# Core data pipeline
pandas>=2.0.0
pyarrow>=14.0.0
pyyaml>=6.0
scikit-learn>=1.3.0

# DeepFace (install TF backend as needed: pip install tf-keras tensorflow)
deepface>=0.0.93

# Dev / tests
pytest>=7.0.0

# Optional: SFT cold-start (see scripts/sft_coldstart.py)
# transformers>=4.50.0
# trl>=0.9.0
# torch>=2.0.0
# accelerate
# peft

# Optional: verl (pin to a recipe’s REQUIRED_VERL / git SHA; example)
# pip install verl  # or: pip install git+https://github.com/verl-project/verl@<sha>

# Optional: vLLM for rollouts in verl (match your CUDA stack)
# vllm>=0.8.2
