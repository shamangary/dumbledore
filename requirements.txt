# Core data pipeline (use pyarrow>=15 and numexpr/bottleneck wheels that match your NumPy major version)
pandas>=2.0.0
pyarrow>=15.0.0
numexpr>=2.10.0
bottleneck>=1.4.0
pyyaml>=6.0
pillow>=9.0.0
# Stage 1 LFW: scikit-learn must use wheels compatible with your NumPy
scikit-learn>=1.3.0

# DeepFace (install TF backend as needed: pip install tf-keras tensorflow)
deepface>=0.0.93

# Dev / tests
pytest>=7.0.0
# Optional: dataset_report.py figures
# matplotlib>=3.7
# pillow>=10.0

# Optional: SFT cold-start (see dumbledore/cli/sft_coldstart.py; run: python -m dumbledore.cli.sft_coldstart)
# transformers>=4.50.0
# trl>=0.9.0
# torch>=2.0.0
# accelerate
# peft

# Optional: verl (pin to a recipe’s REQUIRED_VERL / git SHA; example)
# pip install verl  # or: pip install git+https://github.com/verl-project/verl@<sha>

# Optional: vLLM for rollouts in verl (match your CUDA stack)
# vllm>=0.8.2
